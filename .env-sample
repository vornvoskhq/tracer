# Sample configuration for the Execution Trace Viewer LLM integration.
# Copy this file to ".env" and fill in your actual values.
#
# The tracer app will look for OPENROUTER_API_KEY and OPENROUTER_MODEL either in
# the process environment or in a .env file in the project root.

# Your OpenRouter API key
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Optional: override the default model used for summaries.
# The GUI defaults to "openai/gpt-4o-mini" if this is not set.
# Examples:
#   openai/gpt-4o-mini
#   openai/gpt-4o
#   mistralai/mistral-small
OPENROUTER_MODEL=openai/gpt-4o-mini